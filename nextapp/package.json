{
  "name": "nextapp",
  "version": "0.0.0",
  "private": true,
  "description": "PLACEHOLDER — Next.js + TS (App Router). Scaffolded for real in Stage 5.",
  "scripts": {
    "typecheck": "echo \"[nextapp] placeholder — built in Stage 5\" && exit 0",
    "test": "echo \"[nextapp] placeholder — no tests yet\" && exit 0"
  }
}
